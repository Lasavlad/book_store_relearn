setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    book_store: Dockerfile
release:
  image: book_store
  command:
    - python manage.py collectstatic --noinput
run:
  book_store: gunicorn book_store.wsgi:application